{
    "compilerOptions": {
        "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
        "target": "ES2020",
        "useDefineForClassFields": true,
        // `ES2022.Intl` alone rather than a target bump: `utils/userTimezone`
        // builds the location picker from `Intl.supportedValuesOf("timeZone")`,
        // which is ES2022 and typed nowhere in ES2020. Adding just the Intl
        // slice keeps every other ES2022 feature out of the emitted syntax.
        "lib": ["ES2020", "ES2022.Intl", "DOM", "DOM.Iterable"],
        "module": "ESNext",
        "skipLibCheck": true,

        /* Bundler mode */
        "moduleResolution": "bundler",
        "allowImportingTsExtensions": true,
        "isolatedModules": true,
        "moduleDetection": "force",
        "noEmit": true,
        "jsx": "react-jsx",
        "resolveJsonModule": true,

        /* Linting */
        "strict": true,
        "noUnusedLocals": false,
        "noUnusedParameters": false,
        "noFallthroughCasesInSwitch": true,
        "noUncheckedSideEffectImports": true,
        "forceConsistentCasingInFileNames": true
    },
    "include": ["src"],
    // Tests are validated by running them (vitest), not by the production
    // build. Excluding them keeps stale test-fixture type drift from blocking
    // `npm run build` (and the Railway deploy); vitest still type-aware-runs them.
    "exclude": ["src/__tests__", "src/**/*.test.ts", "src/**/*.test.tsx"]
}
